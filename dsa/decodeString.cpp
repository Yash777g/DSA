#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
using namespace std;

string decodeString(string s) {
    vector<string> st;
    string res = "";

    for(int i = 0; i < s.size(); i++) {
        
    }
    return res;
}

int main() {
    string s = "3[a2[c]]";
    cout << decodeString(s);
    return 0;
}